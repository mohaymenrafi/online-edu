import React from 'react';
import SectionHeaders from '../SectionHeaders/SectionHeaders';

const NotFound = () => {
    return (
        <div>
            <SectionHeaders pageTitle="404 Error! The page is expired."></SectionHeaders>
            {/* <SectionHeaders pageTitle="The page is expired."></SectionHeaders> */}
        </div>
    );
};

export default NotFound;